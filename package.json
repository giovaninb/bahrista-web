{
  "name": "bahrista-web",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "sync:privacy": "node scripts/sync-privacy.mjs",
    "sync:assets": "node scripts/sync-assets.mjs",
    "sync": "npm run sync:assets && npm run sync:privacy",
    "predev": "npm run sync",
    "prebuild": "npm run sync",
    "dev": "astro dev",
    "build": "ASTRO_TELEMETRY_DISABLED=1 astro build",
    "preview": "astro preview"
  },
  "dependencies": {
    "astro": "^5.7.0",
    "marked": "^15.0.7"
  }
}
